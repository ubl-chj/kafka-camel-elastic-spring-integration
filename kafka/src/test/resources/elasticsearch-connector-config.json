{
  "name": "datagen-clickstream_users",
  "config": {
    "connector.class": "io.confluent.connect.elasticsearch.ElasticsearchSinkConnector",
    "type.name": "kafka-connect",
    "topics": "clickstream_users",
    "tasks.max": "1",
    "connection.url": "http://elasticsearch:9200",
    "schema.ignore": "true"
  }
}
